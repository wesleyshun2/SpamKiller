# 向量存储诊断指南（优化版）

## 问题与解决概述

**原始问题**：向量获取失败时，LLM 判定的广告无法保存向量

**解决方案**：采用**一次调用原则** + **向量缓存**

## 🎯 设计原则

### 一次调用原则
```
同一消息在短时间内：
- 第一次自动判定：调用一次向量 API
- 用户手动标记：重新调用向量 API（强制刷新）
```

### 向量缓存机制
- **缓存大小**：最多 500 条
- **有效期**：1 小时
- **Key**：内容哈希值（避免文本重复）
- **Value**：向量 + 时间戳

### 日志优化
- ❌ 不显示向量数组本身
- ✅ 只显示元数据：维度、相似度
- ✅ 显示缓存状态和操作结果

---

## 📊 典型场景对比

### 场景 1: 首次自动判定（缓存未命中）
```
消息到达 → 获取向量（新调用）→ 向量比对 → 保存缓存
  ↓
日志输出：
[向量获取] 文本长度: 143
[嵌入请求] 尝试模型: gemini-embedding-001
✅ 嵌入成功 (維度: 768)
[向量比對] 發現匹配 (相似度: 0.8234)
[判定完成] ⚠️ 廣告 | 來源: Vector | 向量: ✅ 已取得 (維度: 768)
```

### 场景 2: 相同消息再次到达（缓存命中）
```
消息到达 → 检查缓存 → 命中！直接使用
  ↓
日志输出：
[向量获取] 文本长度: 143
[向量缓存] 命中 (存储时间: 25秒前)
[向量比對] 無匹配，將進行 LLM 判定
[...LLM判定...]
```

### 场景 3: 用户手动标记 (/spam 命令)
```
用户执行 /spam → 强制刷新 → 获取新向量 → 保存到数据库
  ↓
日志输出：
[向量獲取] 強制刷新
[嵌入請求] 文本長度: 143
✅ 嵌入成功 (維度: 768)
[保存廣告模式] 新模式，插入資料庫...
✅ 新廣告模式已成功保存
```

---

## ⚙️ 缓存管理

### 自动清理机制
```typescript
// 每 50 条缓存自动清理一次
if (vectorCache.size % 50 === 0) {
    cleanExpiredCache();
}

// 清理策略
1. 删除超过 1 小时的过期条目
2. 如果缓存超过 500 条，删除最旧的 50 条
```

### 监控缓存状态
日志中会显示缓存的年龄：
```
[向量缓存] 命中 (存储时间: 5秒前)      // 很新
[向量缓存] 命中 (存储时间: 1200秒前)   // 20分钟前
[向量缓存] 命中 (存储时间: 3500秒前)   // 接近 1 小时，即将过期
```

---

## 🚀 快速验证步骤

### 第一步：部署新代码
```bash
git add -A
git commit -m "优化: 向量缓存机制 + 一次调用原则"
git push
```

### 第二步：发送测试消息
在监控群组中发送一条可能被判定为广告的消息，观察日志

#### ✅ 预期的成功日志
```
[向量获取] 文本长度: 123
[嵌入请求] 尝试模型: gemini-embedding-001
✅ 嵌入成功 (維度: 768)
[向量比對] 發現匹配 (相似度: 0.85)
[判定完成] ⚠️ 廣告 | 來源: Vector | 向量: ✅ 已取得 (維度: 768)
```

#### ℹ️ 缓存命中日志
```
[向量获取] 文本长度: 123
[向量缓存] 命中 (存储时间: 45秒前)
[向量比對] 已從緩存使用向量進行匹配
```

### 第三步：重复发送相同消息
- 查看是否显示缓存命中
- 验证 API 调用次数减少
- 确认判定结果一致

### 第四步：使用 /spam 手动标记
```
在日志频道回复消息 → /spam
```

预期日志：
```
[向量獲取] 強制刷新
[嵌入請求] 文本長度: 123
✅ 嵌入成功 (維度: 768)
[保存廣告模式] 新模式，插入資料庫...
✅ 新廣告模式已成功保存
```

---

## 🔍 故障排查

### 问题 1: 日志中没有 `[向量缓存]` 消息
**说明**：缓存正常工作（首次或缓存过期时会调用新的嵌入）

### 问题 2: 不断看到新的嵌入请求
**可能原因**：
- 每条消息内容都不同
- 缓存已过期（1 小时）
- 缓存大小超限被清理

**验证**：多次发送**完全相同**的消息，应该看到缓存命中

### 问题 3: 日志中有大量向量数据
**原因**：代码未正确更新

**解决**：确保使用了最新版本（应该只显示维度，不显示数组）

---

## 📈 性能指标

### API 调用成本节省
```
场景：100 条消息，其中 30 条是重复的

旧设计：100 次 API 调用
新设计：70 次 API 调用 (节省 30%)

从 100 条重复的中选 5 条消息各发 10 次：
旧设计：500 次 API 调用
新设计：100 次 API 调用 (节省 80%)!
```

### 缓存命中率预期
```
稳定运行 24 小时内：
- 缓存命中率：~60-80%（取决于消息多样性）
- 平均响应时间：从 1.5s 降至 0.3s
```

---

## 📋 最佳实践

### Do ✅
- 定期观察 `[向量缓存]` 的命中率
- 监控缓存大小不超过 500
- 使用 `/spam` 命令标记关键的垃圾消息

### Don't ❌
- 不要手动清空缓存（自动管理）
- 不要在日志中搜索完整向量（已隐藏）
- 不要期望 100% 的缓存命中（内容会变）

---

## 🎯 验证清单

- [ ] 部署了优化后的代码
- [ ] 发送测试消息，看到向量获取日志
- [ ] 重复发送相同消息，看到缓存命中 `[向量缓存] 命中`
- [ ] 使用 `/spam` 命令，看到强制刷新日志
- [ ] 数据库中有新的 spam_patterns 记录
- [ ] 日志中没有出现完整的向量数组（只有元数据）

---

**版本**: 2.0 (优化版)  
**最后更新**: 2026-02-06

### 情况 2：看到 `❌ All embedding models failed`

**可能原因**：
- Gemini API 配额已用完
- API 密钥无效
- 网络连接问题

**解决方案**：
```
1. 检查 GEMINI_API_KEY 环境变量
2. 登录 Google Cloud Console 检查配额
3. 确认工作环境有网络连接
```

### 情况 3：日志显示成功，但数据库没有新记录

**可能原因**：
- Supabase 连接问题
- RLS 策略限制
- 数据库已满

**解决方案**：
```bash
# 检查日志中是否出现以下错误
# ❌ addSpamPattern insert error

# 如果有错误，查看错误详情
# 检查 RLS 政策是否正确配置
```

### 情况 4：显示 `insert returned no data`

**原因**：`.select()` 调用返回空结果

**状态**：这通常不是错误，向量已正确保存

---

## 📊 性能考虑

### 向量重新获取的成本

- **首次获取失败**：可能是临时 API 错误
- **二次获取**：会消耗额外的 API 配额
- **权衡**：确保重要消息的向量被保存比节省少量 API 调用更重要

### 建议

如果 API 配额经常不足：
```typescript
// 考虑添加更快的降级策略
// 在 gemini.ts 中
if (totalEmbeddingCallsToday > QUOTA_LIMIT * 0.8) {
    console.warn('Approaching quota limit, skipping retry');
    return null;
}
```

---

## 📝 日志记录最佳实践

为帮助快速诊断，日志中使用了不同的前缀：

| 前缀 | 含义 | 示例 |
|------|------|------|
| `✅` | 成功操作 | 向量成功获取 |
| `❌` | 失败操作 | API 调用失败 |
| `⚠️` | 警告信息 | 配额即将用完 |
| `[标签]` | 操作分类 | `[getEmbedding]` |

---

## 🎯 验证清单

- [ ] 部署了新代码
- [ ] 测试消息被标记为广告
- [ ] 日志中出现向量相关的消息
- [ ] 数据库中有新的 `spam_patterns` 记录
- [ ] 使用 `/spam` 命令测试手动标记
- [ ] 确认相似度匹配的消息被正确识别

---

## 📞 需要帮助？

如果问题仍未解决，收集以下信息：

1. **完整的错误日志**（包括时间戳）
2. **测试消息的内容**
3. **Supabase 日志中的任何错误**
4. **API 配额使用情况**

检查命令：
```sql
-- 查看各个表的记录数
SELECT 'spam_patterns' as table_name, COUNT(*) as count FROM spam_patterns
UNION ALL
SELECT 'normal_patterns', COUNT(*) FROM normal_patterns
UNION ALL
SELECT 'violation_logs', COUNT(*) FROM violation_logs;
```

---

**版本**: 1.0  
**最后更新**: 2026-02-06
