# 工作日誌

**AI agents should NOT edit this file**

## 臨時筆記

Webhook 監控語法: 
```
npx.cmd wrangler tail
```


## 🚀 待修正事項

1. 確認訊息檢查邏輯：
   - 所有訊息都先向量化，比對後不確定才由 LLM 判定。
2. 更新機器人回報的紀錄事項
   - 紀錄內容增加 embadded 模型的工作狀態
   - 紀錄只發到轉發頻道
   - 紀錄不發佈到管理頻道，維持版面乾淨
3. 確認向量資料庫可以自動更新
   - 管理員主動回報:廣告與非廣告
   - 如何標記到資料庫
   - 累積大量案例後的處理方法
4. 改良違規次數計算邏輯
   - 刪除資料庫違規紀錄的次數欄
5. 測試 gemma3 4B 模型的測試表現
6. 改善指令及輸入操作方式
   - 可以指定控制指令頻道，預設從轉發頻道控制
   - 操作指令使用圖形化介面，輸入變數時才要求文字輸入
7. 檢查多餘的資料庫欄位


## 2026.02.05 工作進度摘要：AI 模型過時

### 可用模型
 **先做向量化並比對**：gemini-embedding-001
 **不確定才用 LLM 模型**：
     - 優先使用 gemini-2.5-flash-lite
     - 配額用盡則使用 gemma-3-4b-it
     - 若 AI 判定為 Spam，將向量存回資料庫。

### 已停用模型
    text-embedding-004
    所有 gemma 2 系列模型
    所有 Gemini 2 系列模型


## 2026.02.04 重構與偵錯

### 1. 核心 Pipeline 重構 (v2.0)
- **統一處理**：透過 `bot.on(['message', edited_message'])` 確保新訊息與編輯過的訊息都會進入過濾流程。
- **雙層判斷架構**：
  - **Level 1 (Vector)**：先進行 Supabase 向量比對，若相似度 > 0.85 則直接判定為 Spam。
  - **Level 2 (LLM)**：若無向量匹配，則呼叫 Gemini 進行語意分析；若 AI 判定為 Spam，系統會自動學習並將特徵存回向量庫。

### 2. 強韌的 API 備援機制 (解決 404/429 問題)
- **多模型輪詢 (Fallback)**：
  嘗試 `gemini-2.5-flash` -> `2.0-Lite` -> `1.5-flash-latest` -> `1.5-pro-latest` -> `gemini-pro`。
- **429 (配額) 處理**：當偵測到配額滿了，會立刻嘗試清單中的下一個模型。

### 3. 設定與轉發系統優化
- **解決設定遺失**：修復了 `/config` 解析空格的問題，現在會自動 `trim()` 並忽略多餘空格。

### 4. 監控邏輯修正
- 修復了監控開關 (Toggle) 的一致性問題，並確保 `/monitor` 回應語法直覺。
- 增加日誌監控 `[Logging Check]` 與 `[Monitor Check]`，可透過 `npx wrangler tail` 即時觀測。
