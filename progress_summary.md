# 2026-02-04/05 工作進度摘要：SpamKiller 重構與偵錯

這份文件記錄了今日的重大更動與目前的系統狀態，方便後續接手測試。
Webhook 監控語法: `npx.cmd wrangler tail`

## 🛠️ 已完成的重大修復與功能

### 1. 核心 Pipeline 重構 (v2.0)
- **統一處理**：透過 `bot.on(['message', 'edited_message'])` 確保新訊息與編輯過的訊息都會進入過濾流程。
- **雙層判斷架構**：
  - **Level 1 (Vector)**：先進行 Supabase 向量比對，若相似度 > 0.85 則直接判定為 Spam。
  - **Level 2 (LLM)**：若無向量匹配，則呼叫 Gemini 進行語意分析；若 AI 判定為 Spam，系統會自動學習並將特徵存回向量庫。

### 2. 強韌的 API 備援機制 (解決 404/429 問題)
- **多模型輪詢 (Fallback)**：`GeminiService` 現在會依序嘗試 `gemini-2.0-flash` -> `2.0-Lite` -> `1.5-flash-latest` -> `1.5-pro-latest` -> `gemini-pro`。
- **API 版本自動切換**：若在 `v1beta` 遇到 404 (Not Found)，會自動切換至 `v1` 穩定版再次嘗試。
- **429 (配額) 處理**：當偵測到配額滿了，會立刻嘗試清單中的下一個模型。

### 3. 設定與轉發系統優化
- **解決設定遺失**：修復了 `/config` 解析空格的問題，現在會自動 `trim()` 並忽略多餘空格。
- **智慧錯誤提示**：
  - 若轉發失敗（如 ID 錯誤或機器人被踢出頻道），機器人會直接在監控群組回報具體錯誤（例如：`Chat not found`）。
  - 自動處理「群組升級 (Chat Upgraded)」導致 ID 變更的情況。

### 4. 監控邏輯修正
- 修復了監控開關 (Toggle) 的一致性問題，並確保 `/monitor` 回應語法直覺。
- 增加日誌監控 `[Logging Check]` 與 `[Monitor Check]`，可透過 `npx wrangler tail` 即時觀測。

---

## 📌 當前系統狀態與剩餘問題

- **轉發頻道**：已修復完成，目前可穩定收到「[OK] 判定結果」或「[EX] 判定結果」回報。
- **API 狀態 (待解項目)**：
  - **429 Error**：您的 `gemini-2.0-flash` 目前處於配額耗盡狀態。
  - **404 Error**：即便使用 `v1` 備援，目前 1.5 系列的模型依然全部回報 404（疑似您的 Google 專案完全切斷了 1.5 系列的權限）。
  - **Fallback 策略**：當所有模型都失敗時，機器人會執行 **「API Error Fallback」**：為了不影響使用者說話，它會「暫時放行」該訊息，但依然會轉發至 Log Channel 並標註錯誤，確保可追溯性。

---

## 🚀 明日測試指引

1.  **檢查 API 配額**：隔天 2.0 模型的配額重置後，判定功能應會恢復。
2.  **測試自動學習**：發送一段廣告，看看 Log Channel 是否顯示「[EX] 判定結果」且「核心: Gemini LLM」。
3.  **確認向量攔截**：再次發送內容雷同的廣告，看看「核心」是否變成了「Vector」，這代表自動學習成功。
4.  **注意指令**：若機器人沒反應，先 `/ping` 確認它活著，再檢查 `/monitor` 是否開啟。
